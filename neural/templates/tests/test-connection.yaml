apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "neural.fullname" . }}-test-connection"
  labels:
    {{- include "neural.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "neural.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
